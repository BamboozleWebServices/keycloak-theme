{
    "name": "bamboozle-keycloak-theme",
    "version": "1.0.0",
    "description": "Bamboozle branded Keycloak login theme (child of Keywind)",
    "private": true,
    "scripts": {
          "build": "cd node_modules/keywind && pnpm install && pnpm build && cd ../..",
          "build:jar": "node scripts/build-jar.mjs"
    },
    "devDependencies": {
          "keywind": "github:lukin/keywind"
    }
}
